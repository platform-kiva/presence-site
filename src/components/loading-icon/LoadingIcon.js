// styles
import { LoadingIconOverlay, LoadingIconContainer} from './LoadingIcon.styles.js'

export default function LoadingIcon() {
  return (
    <LoadingIconOverlay>
        <LoadingIconContainer />
    </LoadingIconOverlay>
  )
};